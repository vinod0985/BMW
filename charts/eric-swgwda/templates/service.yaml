apiVersion: v1
kind: Service
metadata:
  name: {{ include "eric-swgwda.fullname" . }}
  labels:
    {{- include "eric-swgwda.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eric-swgwda.selectorLabels" . | nindent 4 }}
