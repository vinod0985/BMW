apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eric-swgwda.fullname" . }}-test-connection"
  labels:
    {{- include "eric-swgwda.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eric-swgwda.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
